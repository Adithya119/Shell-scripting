#!/bin/bash

for course in DevOps AWS Azure Linux Windows ;               # course, not courses (singular, not plural);    # no commas
do
   echo ${course}                                         # echo "${course}" also works
done